# EventLoop
JS is synchronous and single thereaded bydefault

## There can be async behaviour
- with BrowserAPI -
setTimeout,setInternal,
setImmediate,nextTick
- With promises
- With event handlers
## Asynchronous
A function not exicuted emidiately but it must be exicuted after a while . It has some status during the exicution .
At final it may resolve(success) or reject (unsuccess) .
## callback -
That pass as argument or the parameter to another function .
## Modern js is divided in two types
common js (.cjs) -> support OOPS
modulejs.  (.mjs) -> follow moduler approach -> important